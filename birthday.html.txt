<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Happy Birthday Xuân Thư 🎂</title>
<style>
@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;900&family=Quicksand:wght@500;600;700&display=swap');

*{margin:0;padding:0;box-sizing:border-box;}

body {
  min-height: 100vh;
  background: #000;
  display: flex;
  flex-direction: column;
  align-items: center;
  font-family: 'Quicksand', sans-serif;
  overflow-x: hidden;
}

.bg-wrap {
  position: relative;
  width: 100%;
  max-width: 500px;
  margin: 0 auto;
}

.bg-img {
  width: 100%;
  display: block;
}

.polaroid-wrap {
  position: absolute;
  top: 28%;
  left: 50%;
  transform: translateX(-50%);
  width: 52%;
  z-index: 10;
}

.polaroid {
  background: #f0ede8;
  padding: 10px 10px 38px 10px;
  box-shadow: 0 8px 30px rgba(0,0,0,0.5), 0 2px 8px rgba(0,0,0,0.3);
  border-radius: 2px;
  position: relative;
}

.polaroid img {
  width: 100%;
  aspect-ratio: 1/1;
  object-fit: cover;
  object-position: top center;
  display: block;
}

.polaroid-text {
  position: absolute;
  bottom: 6px;
  left: 10px;
  right: 10px;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.polaroid-hb {
  font-family: 'Playfair Display', serif;
  font-size: clamp(10px, 3vw, 15px);
  font-weight: 900;
  color: #1a1a1a;
  line-height: 1.2;
}

.polaroid-heart {
  font-size: clamp(12px, 3.5vw, 18px);
  color: #1a1a1a;
}

.info-section {
  width: 100%;
  max-width: 500px;
  background: linear-gradient(180deg, #0d0d0d 0%, #1a0a1a 50%, #0d0d0d 100%);
  padding: 28px 24px 40px;
  text-align: center;
  position: relative;
  overflow: hidden;
}

.info-section::before {
  content: '';
  position: absolute;
  inset: 0;
  background: radial-gradient(ellipse at 50% 0%, rgba(255,105,180,0.15) 0%, transparent 60%);
  pointer-events: none;
}

.name-wrap {
  margin-bottom: 20px;
  position: relative;
  z-index: 2;
}

.name-label {
  font-size: 11px;
  letter-spacing: 4px;
  text-transform: uppercase;
  color: rgba(255,255,255,0.4);
  margin-bottom: 6px;
}

.name-main {
  font-family: 'Playfair Display', serif;
  font-size: clamp(22px, 6vw, 34px);
  font-weight: 900;
  color: #fff;
  text-shadow: 0 0 20px rgba(255,105,180,0.6);
  line-height: 1.2;
}

.divider {
  display: flex;
  align-items: center;
  gap: 10px;
  margin: 18px 0;
  position: relative;
  z-index: 2;
}
.divider::before,.divider::after {
  content: '';
  flex: 1;
  height: 1px;
  background: linear-gradient(to right, transparent, rgba(255,215,0,0.4), transparent);
}
.divider-text {
  color: rgba(255,215,0,0.7);
  font-size: 14px;
  letter-spacing: 4px;
}

.info-cards {
  display: flex;
  flex-direction: column;
  gap: 10px;
  margin-bottom: 20px;
  position: relative;
  z-index: 2;
}

.info-card {
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 12px;
  padding: 12px 16px;
  display: flex;
  align-items: center;
  gap: 12px;
  text-align: left;
}

.info-icon { font-size: 20px; flex-shrink: 0; }
.info-lbl {
  font-size: 10px;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: rgba(255,215,0,0.6);
  margin-bottom: 2px;
}
.info-val {
  font-size: 13px;
  color: #f0e6ff;
  font-weight: 600;
  line-height: 1.4;
}

.wish {
  background: rgba(255,105,180,0.08);
  border: 1px dashed rgba(255,105,180,0.3);
  border-radius: 14px;
  padding: 16px 18px;
  font-size: 14px;
  color: #e0c8ff;
  line-height: 1.8;
  font-weight: 500;
  position: relative;
  z-index: 2;
  margin-bottom: 20px;
}

.emoji-row {
  font-size: 24px;
  letter-spacing: 8px;
  animation: bounce 2s ease-in-out infinite;
  position: relative;
  z-index: 2;
}
@keyframes bounce{0%,100%{transform:translateY(0);}50%{transform:translateY(-6px);}}

canvas {
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 100;
  width: 100%;
  height: 100%;
}
</style>
</head>
<body>

<canvas id="confetti"></canvas>

<div class="bg-wrap">
  <!-- ẢNH NỀN -->
  <img class="bg-img" src="nen.jpg" alt="background" />

  <div class="polaroid-wrap">
    <div class="polaroid">
      <!-- ẢNH XUÂN THƯ -->
      <img src="thu.jpg" alt="Xuân Thư" />
      <div class="polaroid-text">
        <div class="polaroid-hb">Happy<br>Birthday</div>
        <div class="polaroid-heart">♡</div>
      </div>
    </div>
  </div>
</div>

<div class="info-section">
  <div class="name-wrap">
    <div class="name-label">✦ sinh nhật ✦</div>
    <div class="name-main">Nguyễn Lâm Xuân Thư</div>
  </div>

  <div class="divider"><span class="divider-text">18 · 06 · 2026</span></div>

  <div class="info-cards">
    <div class="info-card">
      <div class="info-icon">🎉</div>
      <div class="info-body">
        <div class="info-lbl">Sự kiện</div>
        <div class="info-val">Nhiệt Liệt Chào Mừng Tuổi Mới · Khóa 2026</div>
      </div>
    </div>
    <div class="info-card">
      <div class="info-icon">📍</div>
      <div class="info-body">
        <div class="info-lbl">Quê hương</div>
        <div class="info-val">Người con của Lagi · Bình Thuận 🌊</div>
      </div>
    </div>
  </div>

  <div class="wish">
    🌸 Chúc Thư một tuổi mới thật rực rỡ,<br>
    tràn đầy yêu thương & niềm vui!<br>
    Mãi xinh đẹp, mãi tỏa sáng nhé! 💖✨
  </div>

  <div class="emoji-row">🎂 🎁 🎈 🥳 🎊</div>
</div>

<script>
const canvas=document.getElementById('confetti');
const ctx=canvas.getContext('2d');
canvas.width=window.innerWidth;canvas.height=window.innerHeight;
const cols=['#ff69b4','#ffd700','#fff','#ff1493','#c084fc','#60a5fa'];
const pieces=[];
for(let i=0;i<60;i++){
  pieces.push({x:Math.random()*canvas.width,y:Math.random()*canvas.height-canvas.height,w:Math.random()*8+3,h:Math.random()*5+2,color:cols[Math.floor(Math.random()*cols.length)],vx:(Math.random()-0.5)*1.5,vy:Math.random()*1.5+0.5,rot:Math.random()*360,vrot:(Math.random()-0.5)*3});
}
function draw(){
  ctx.clearRect(0,0,canvas.width,canvas.height);
  pieces.forEach(p=>{
    p.x+=p.vx;p.y+=p.vy;p.rot+=p.vrot;
    if(p.y>canvas.height){p.y=-10;p.x=Math.random()*canvas.width;}
    ctx.save();ctx.translate(p.x,p.y);ctx.rotate(p.rot*Math.PI/180);
    ctx.globalAlpha=0.7;ctx.fillStyle=p.color;
    ctx.fillRect(-p.w/2,-p.h/2,p.w,p.h);ctx.restore();
  });
  requestAnimationFrame(draw);
}
draw();
window.addEventListener('resize',()=>{canvas.width=window.innerWidth;canvas.height=window.innerHeight;});
</script>
</body>
</html>